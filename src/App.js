import "./App.css";
import Testupload from "./Testupload";
window.Buffer = window.Buffer || require("buffer").Buffer;

function App() {
  return (
    <div>
      <Testupload />
    </div>
  );
}

export default App;
